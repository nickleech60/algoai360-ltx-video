runpod>=1.6.0
# PROVEN-WORKING LTX-Video stack (verified against the community reference env):
#   torch 2.5.1 + transformers 4.47.1 + diffusers 0.33.0 + accelerate 1.2.1.
# Two earlier failures taught the version rules the hard way:
#   - torch 2.4.0 lacked torch.distributed.tensor.DTensor → import crash. Need >=2.5.
#   - pinning diffusers/transformers BACKWARD (0.32.2 / 4.44.2) hit
#     "cannot import name FLAX_WEIGHTS_NAME" — the LTX pipeline needs the NEWER pair.
# So we move FORWARD to the known-good combo, not backward.
torch==2.5.1
transformers==4.47.1
diffusers==0.33.0
accelerate==1.2.1
sentencepiece
imageio[ffmpeg]>=2.34.0
imageio-ffmpeg
numpy
pillow
boto3
